import { VisierObjectConfiguration } from "./index";

describe("visier-object-configuration-typescript-sdk", () => {
    it("initialize client", async () => {
        const visierobjectconfiguration = new VisierObjectConfiguration({
        });
    });
});
